const publicURL = "";
const imageBase = publicURL + "/images";
export const Resources = {
  Images: {
    dp: `${imageBase}/dp.jpg`,
  },
};

export const baseURL = "https://portfolio-be-cfxq.onrender.com";
